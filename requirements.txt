# ─────────────────────────────────────────────────────────────
#  python-face-service – requirements.txt
#  Compatible with Python 3.13 on Windows (pre-built wheels only)
# ─────────────────────────────────────────────────────────────

# ── Core Framework ────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── Computer Vision ───────────────────────────────────────────
# opencv-python has pre-built wheels for Python 3.13
opencv-python>=4.10.0.84

# numpy 2.x has pre-built wheels for Python 3.13 on Windows
numpy>=2.0.0

# Pillow (image processing)
Pillow>=10.4.0

# ── Face Recognition ──────────────────────────────────────────
# face_recognition + dlib have NO pre-built wheels for Python 3.11 on Windows.
# Building dlib from source requires MSVC C++ workload which is not present.
# We use MediaPipe (Google) instead — pre-built wheels, no compiler needed,
# compatible with Python 3.11 + NumPy 2.x + OpenCV 5.x on Windows AMD64.
mediapipe==0.10.14

# dlib and face-recognition are left here for reference only.
# Do NOT install on Windows/Python 3.11 without a full MSVC C++ build toolchain.
# dlib>=19.24.0
# face-recognition>=1.3.0

# ── Database ──────────────────────────────────────────────────
SQLAlchemy>=2.0.32
alembic>=1.13.2

# ── HTTP Client ───────────────────────────────────────────────
requests>=2.32.3
httpx>=0.27.2

# ── Configuration & Validation ────────────────────────────────
python-dotenv>=1.0.1
pydantic>=2.8.2
pydantic-settings>=2.4.0

# ── Async Utilities ───────────────────────────────────────────
aiofiles>=24.1.0
